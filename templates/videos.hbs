{{#if videos}}
  {{#each videos}}
    <div class="video" data-href="http://www.youtube.com/watch?v={{this.id.videoId}}">
      <img src="{{this.snippet.thumbnails.default.url}}">
      <span>{{this.snippet.title}}</span>
    </div>
  {{/each}}
{{else}}
  <p>No videos found.</p>
{{/if}}
