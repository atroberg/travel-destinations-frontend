{{! don't print whitespace, because it adds spacing between the images! }}
{{#each photos}}<img src="{{this.src}}" data-big-src="{{this.bigSrc}}" data-xbig-src="{{this.xbigSrc}}" title="{{this.title}}">{{/each}}
